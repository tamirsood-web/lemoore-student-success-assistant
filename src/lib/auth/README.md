# lib/auth/ (reserved)

Reserved for Cognito JWT verification and session helpers. **Not implemented in the local
MVP scaffold.** The local MVP runs anonymously with no authentication. This is a
placeholder only so the structure matches `.kiro/steering/structure.md`; the real
implementation lands in a later AWS phase.
