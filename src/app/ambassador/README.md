# ambassador/ (reserved)

Reserved for the Cognito-protected ambassador experience. **Not implemented in the local
MVP scaffold.** See `.kiro/specs/local-mvp-scaffold/` — this route is out of scope for the
current spec and is a placeholder only so the directory structure matches
`.kiro/steering/structure.md`.
